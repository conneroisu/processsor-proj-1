.data
.text
    .globlmain

main:
    #Start test
    xori    $1,     $0,     1   # verify that one can set 1 to the register and 0 xori 1 works in ALU
    xori    $2,     $0,     1   # verify that one can set 1 to the register and 0 xori 1 works in ALU
    xori    $3,     $0,     1   # verify that one can set 1 to the register and 0 xori 1 works in ALU
    xori    $4,     $0,     1   # verify that one can set 1 to the register and 0 xori 1 works in ALU
    xori    $5,     $0,     1   # verify that one can set 1 to the register and 0 xori 1 works in ALU
    xori    $6,     $0,     1   # verify that one can set 1 to the register and 0 xori 1 works in ALU
    xori    $7,     $0,     1   # verify that one can set 1 to the register and 0 xori 1 works in ALU
    xori    $8,     $0,     1   # verify that one can set 1 to the register and 0 xori 1 works in ALU
    xori    $9,     $0,     1   # verify that one can set 1 to the register and 0 xori 1 works in ALU
    xori    $10,    $0,     1   # verify that one can set 1 to the register and 0 xori 1 works in ALU
    xori    $11,    $0,     1   # verify that one can set 1 to the register and 0 xori 1 works in ALU
    xori    $12,    $0,     1   # verify that one can set 1 to the register and 0 xori 1 works in ALU
    xori    $13,    $0,     1   # verify that one can set 1 to the register and 0 xori 1 works in ALU
    xori    $14,    $0,     1   # verify that one can set 1 to the register and 0 xori 1 works in ALU
    xori    $15,    $0,     1   # verify that one can set 1 to the register and 0 xori 1 works in ALU
    xori    $16,    $0,     1   # verify that one can set 1 to the register and 0 xori 1 works in ALU
    xori    $17,    $0,     1   # verify that one can set 1 to the register and 0 xori 1 works in ALU
    xori    $18,    $0,     1   # verify that one can set 1 to the register and 0 xori 1 works in ALU
    xori    $19,    $0,     1   # verify that one can set 1 to the register and 0 xori 1 works in ALU
    xori    $20,    $0,     1   # verify that one can set 1 to the register and 0 xori 1 works in ALU
    xori    $21,    $0,     1   # verify that one can set 1 to the register and 0 xori 1 works in ALU
    xori    $22,    $0,     1   # verify that one can set 1 to the register and 0 xori 1 works in ALU
    xori    $23,    $0,     1   # verify that one can set 1 to the register and 0 xori 1 works in ALU
    xori    $24,    $0,     1   # verify that one can set 1 to the register and 0 xori 1 works in ALU
    xori    $25,    $0,     1   # verify that one can set 1 to the register and 0 xori 1 works in ALU
    xori    $26,    $0,     1   # verify that one can set 1 to the register and 0 xori 1 works in ALU
    xori    $27,    $0,     1   # verify that one can set 1 to the register and 0 xori 1 works in ALU
    xori    $28,    $0,     1   # verify that one can set 1 to the register and 0 xori 1 works in ALU
    xori    $29,    $0,     1   # verify that one can set 1 to the register and 0 xori 1 works in ALU
    xori    $30,    $0,     1   # verify that one can set 1 to the register and 0 xori 1 works in ALU
    xori    $31,    $0,     1   # verify that one can set 1 to the register and 0 xori 1 works in ALU

    xori    $1,     $1,     0   # verify that the register stored value is 1 and 1 xori 0 works in ALU
    xori    $2,     $2,     0   # verify that the register stored value is 1 and 1 xori 0 works in ALU
    xori    $3,     $3,     0   # verify that the register stored value is 1 and 1 xori 0 works in ALU
    xori    $4,     $4,     0   # verify that the register stored value is 1 and 1 xori 0 works in ALU
    xori    $5,     $5,     0   # verify that the register stored value is 1 and 1 xori 0 works in ALU
    xori    $6,     $6,     0   # verify that the register stored value is 1 and 1 xori 0 works in ALU
    xori    $7,     $7,     0   # verify that the register stored value is 1 and 1 xori 0 works in ALU
    xori    $8,     $8,     0   # verify that the register stored value is 1 and 1 xori 0 works in ALU
    xori    $9,     $9,     0   # verify that the register stored value is 1 and 1 xori 0 works in ALU
    xori    $10,    $10,    0   # verify that the register stored value is 1 and 1 xori 0 works in ALU
    xori    $11,    $11,    0   # verify that the register stored value is 1 and 1 xori 0 works in ALU
    xori    $12,    $12,    0   # verify that the register stored value is 1 and 1 xori 0 works in ALU
    xori    $13,    $13,    0   # verify that the register stored value is 1 and 1 xori 0 works in ALU
    xori    $14,    $14,    0   # verify that the register stored value is 1 and 1 xori 0 works in ALU
    xori    $15,    $15,    0   # verify that the register stored value is 1 and 1 xori 0 works in ALU
    xori    $16,    $16,    0   # verify that the register stored value is 1 and 1 xori 0 works in ALU
    xori    $17,    $17,    0   # verify that the register stored value is 1 and 1 xori 0 works in ALU
    xori    $18,    $18,    0   # verify that the register stored value is 1 and 1 xori 0 works in ALU
    xori    $19,    $19,    0   # verify that the register stored value is 1 and 1 xori 0 works in ALU
    xori    $20,    $20,    0   # verify that the register stored value is 1 and 1 xori 0 works in ALU
    xori    $21,    $21,    0   # verify that the register stored value is 1 and 1 xori 0 works in ALU
    xori    $22,    $22,    0   # verify that the register stored value is 1 and 1 xori 0 works in ALU
    xori    $23,    $23,    0   # verify that the register stored value is 1 and 1 xori 0 works in ALU
    xori    $24,    $24,    0   # verify that the register stored value is 1 and 1 xori 0 works in ALU
    xori    $25,    $25,    0   # verify that the register stored value is 1 and 1 xori 0 works in ALU
    xori    $26,    $26,    0   # verify that the register stored value is 1 and 1 xori 0 works in ALU
    xori    $27,    $27,    0   # verify that the register stored value is 1 and 1 xori 0 works in ALU
    xori    $28,    $28,    0   # verify that the register stored value is 1 and 1 xori 0 works in ALU
    xori    $29,    $29,    0   # verify that the register stored value is 1 and 1 xori 0 works in ALU
    xori    $30,    $30,    0   # verify that the register stored value is 1 and 1 xori 0 works in ALU
    xori    $31,    $31,    0   # verify that the register stored value is 1 and 1 xori 0 works in ALU

    xori    $1,     $1,     1   # verify that the register stored value is 0 and 1 xori 1 works in ALU
    xori    $2,     $2,     1   # verify that the register stored value is 0 and 1 xori 1 works in ALU
    xori    $3,     $3,     1   # verify that the register stored value is 0 and 1 xori 1 works in ALU
    xori    $4,     $4,     1   # verify that the register stored value is 0 and 1 xori 1 works in ALU
    xori    $5,     $5,     1   # verify that the register stored value is 0 and 1 xori 1 works in ALU
    xori    $6,     $6,     1   # verify that the register stored value is 0 and 1 xori 1 works in ALU
    xori    $7,     $7,     1   # verify that the register stored value is 0 and 1 xori 1 works in ALU
    xori    $8,     $8,     1   # verify that the register stored value is 0 and 1 xori 1 works in ALU
    xori    $9,     $9,     1   # verify that the register stored value is 0 and 1 xori 1 works in ALU
    xori    $10,    $10,    1   # verify that the register stored value is 0 and 1 xori 1 works in ALU
    xori    $11,    $11,    1   # verify that the register stored value is 0 and 1 xori 1 works in ALU
    xori    $12,    $12,    1   # verify that the register stored value is 0 and 1 xori 1 works in ALU
    xori    $13,    $13,    1   # verify that the register stored value is 0 and 1 xori 1 works in ALU
    xori    $14,    $14,    1   # verify that the register stored value is 0 and 1 xori 1 works in ALU
    xori    $15,    $15,    1   # verify that the register stored value is 0 and 1 xori 1 works in ALU
    xori    $16,    $16,    1   # verify that the register stored value is 0 and 1 xori 1 works in ALU
    xori    $17,    $17,    1   # verify that the register stored value is 0 and 1 xori 1 works in ALU
    xori    $18,    $18,    1   # verify that the register stored value is 0 and 1 xori 1 works in ALU
    xori    $19,    $19,    1   # verify that the register stored value is 0 and 1 xori 1 works in ALU
    xori    $20,    $20,    1   # verify that the register stored value is 0 and 1 xori 1 works in ALU
    xori    $21,    $21,    1   # verify that the register stored value is 0 and 1 xori 1 works in ALU
    xori    $22,    $22,    1   # verify that the register stored value is 0 and 1 xori 1 works in ALU
    xori    $23,    $23,    1   # verify that the register stored value is 0 and 1 xori 1 works in ALU
    xori    $24,    $24,    1   # verify that the register stored value is 0 and 1 xori 1 works in ALU
    xori    $25,    $25,    1   # verify that the register stored value is 0 and 1 xori 1 works in ALU
    xori    $26,    $26,    1   # verify that the register stored value is 0 and 1 xori 1 works in ALU
    xori    $27,    $27,    1   # verify that the register stored value is 0 and 1 xori 1 works in ALU
    xori    $28,    $28,    1   # verify that the register stored value is 0 and 1 xori 1 works in ALU
    xori    $29,    $29,    1   # verify that the register stored value is 0 and 1 xori 1 works in ALU
    xori    $30,    $30,    1   # verify that the register stored value is 0 and 1 xori 1 works in ALU
    xori    $31,    $31,    1   # verify that the register stored value is 0 and 1 xori 1 works in ALU

    # End Test

    halt    
