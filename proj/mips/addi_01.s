.data
.text
    .globlmain
main:
    # Start Test
    addi    $1,     $0, 32767   # verify that one can sign extend for negative numbers
    addi    $2,     $0, 32767   # verify that one can sign extend for negative numbers
    addi    $3,     $0, 32767   # verify that one can sign extend for negative numbers
    addi    $4,     $0, 32767   # verify that one can sign extend for negative numbers
    addi    $5,     $0, 32767   # verify that one can sign extend for negative numbers
    addi    $6,     $0, 32767   # verify that one can sign extend for negative numbers
    addi    $7,     $0, 32767   # verify that one can sign extend for negative numbers
    addi    $8,     $0, 32767   # verify that one can sign extend for negative numbers
    addi    $9,     $0, 32767   # verify that one can sign extend for negative numbers
    addi    $10,    $0, 32767   # verify that one can sign extend for negative numbers
    addi    $11,    $0, 32767   # verify that one can sign extend for negative numbers
    addi    $12,    $0, 32767   # verify that one can sign extend for negative numbers
    addi    $13,    $0, 32767   # verify that one can sign extend for negative numbers
    addi    $14,    $0, 32767   # verify that one can sign extend for negative numbers
    addi    $15,    $0, 32767   # verify that one can sign extend for negative numbers
    addi    $16,    $0, 32767   # verify that one can sign extend for negative numbers
    addi    $17,    $0, 32767   # verify that one can sign extend for negative numbers
    addi    $18,    $0, 32767   # verify that one can sign extend for negative numbers
    addi    $19,    $0, 32767   # verify that one can sign extend for negative numbers
    addi    $20,    $0, 32767   # verify that one can sign extend for negative numbers
    addi    $21,    $0, 32767   # verify that one can sign extend for negative numbers
    addi    $22,    $0, 32767   # verify that one can sign extend for negative numbers
    addi    $23,    $0, 32767   # verify that one can sign extend for negative numbers
    addi    $24,    $0, 32767   # verify that one can sign extend for negative numbers
    addi    $25,    $0, 32767   # verify that one can sign extend for negative numbers
    addi    $26,    $0, 32767   # verify that one can sign extend for negative numbers
    addi    $27,    $0, 32767   # verify that one can sign extend for negative numbers
    addi    $28,    $0, 32767   # verify that one can sign extend for negative numbers
    addi    $29,    $0, 32767   # verify that one can sign extend for negative numbers
    addi    $30,    $0, 32767   # verify that one can sign extend for negative numbers
    addi    $31,    $0, 32767   # verify that one can sign extend for negative numbers
    # End Test

    # Exit program
    halt    
    li      $v0,    10
    syscall 
