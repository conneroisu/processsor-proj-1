.data
.text
    .globlmain
main:
    # Start Test
    addi    $1,     $0, -1  # verify that one can sign extend for positive numbers
    addi    $2,     $0, -1  # verify that one can sign extend for positive numbers
    addi    $3,     $0, -1  # verify that one can sign extend for positive numbers
    addi    $4,     $0, -1  # verify that one can sign extend for positive numbers
    addi    $5,     $0, -1  # verify that one can sign extend for positive numbers
    addi    $6,     $0, -1  # verify that one can sign extend for positive numbers
    addi    $7,     $0, -1  # verify that one can sign extend for positive numbers
    addi    $8,     $0, -1  # verify that one can sign extend for positive numbers
    addi    $9,     $0, -1  # verify that one can sign extend for positive numbers
    addi    $10,    $0, -1  # verify that one can sign extend for positive numbers
    addi    $11,    $0, -1  # verify that one can sign extend for positive numbers
    addi    $12,    $0, -1  # verify that one can sign extend for positive numbers
    addi    $13,    $0, -1  # verify that one can sign extend for positive numbers
    addi    $14,    $0, -1  # verify that one can sign extend for positive numbers
    addi    $15,    $0, -1  # verify that one can sign extend for positive numbers
    addi    $16,    $0, -1  # verify that one can sign extend for positive numbers
    addi    $17,    $0, -1  # verify that one can sign extend for positive numbers
    addi    $18,    $0, -1  # verify that one can sign extend for positive numbers
    addi    $19,    $0, -1  # verify that one can sign extend for positive numbers
    addi    $20,    $0, -1  # verify that one can sign extend for positive numbers
    addi    $21,    $0, -1  # verify that one can sign extend for positive numbers
    addi    $22,    $0, -1  # verify that one can sign extend for positive numbers
    addi    $23,    $0, -1  # verify that one can sign extend for positive numbers
    addi    $24,    $0, -1  # verify that one can sign extend for positive numbers
    addi    $25,    $0, -1  # verify that one can sign extend for positive numbers
    addi    $26,    $0, -1  # verify that one can sign extend for positive numbers
    addi    $27,    $0, -1  # verify that one can sign extend for positive numbers
    addi    $28,    $0, -1  # verify that one can sign extend for positive numbers
    addi    $29,    $0, -1  # verify that one can sign extend for positive numbers
    addi    $30,    $0, -1  # verify that one can sign extend for positive numbers
    addi    $31,    $0, -1  # verify that one can sign extend for positive numbers
    # End Test

    # Exit program
    halt    
    li      $v0,    10
    syscall 
