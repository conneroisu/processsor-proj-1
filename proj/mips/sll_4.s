addi    $1,     $0,     0xFFFFFFFF  # initialize register 1 to all F's
sll     $1,     $1,     31          # shift left by 32 bits should be left all 0's

addi    $1,     $0,     0xFFFFFFFF  # initialize register 1 to all F's
sll     $2,     $2,     31          # shift left by 32 bits should be left all 0's

addi    $3,     $0,     0xFFFFFFFF  # initialize register 1 to all F's
sll     $3,     $3,     31          # shift left by 32 bits should be left all 0's

addi    $4,     $0,     0xFFFFFFFF  # initialize register 1 to all F's
sll     $4,     $4,     31          # shift left by 32 bits should be left all 0's

addi    $5,     $0,     0xFFFFFFFF  # initialize register 1 to all F's
sll     $5,     $5,     31          # shift left by 32 bits should be left all 0's

addi    $6,     $0,     0xFFFFFFFF  # initialize register 1 to all F's
sll     $6,     $6,     31          # shift left by 32 bits should be left all 0's

addi    $7,     $0,     0xFFFFFFFF  # initialize register 1 to all F's
sll     $7,     $7,     31          # shift left by 32 bits should be left all 0's

addi    $8,     $0,     0xFFFFFFFF  # initialize register 1 to all F's
sll     $8,     $8,     31          # shift left by 32 bits should be left all 0's

addi    $9,     $0,     0xFFFFFFFF  # initialize register 1 to all F's
sll     $9,     $9,     31          # shift left by 32 bits should be left all 0's

addi    $10,    $0,     0xFFFFFFFF  # initialize register 1 to all F's
sll     $10,    $10,    31          # shift left by 32 bits should be left all 0's

addi    $11,    $0,     0xFFFFFFFF  # initialize register 1 to all F's
sll     $11,    $11,    31          # shift left by 32 bits should be left all 0's

addi    $12,    $0,     0xFFFFFFFF  # initialize register 1 to all F's
sll     $12,    $12,    31          # shift left by 32 bits should be left all 0's

addi    $13,    $0,     0xFFFFFFFF  # initialize register 1 to all F's
sll     $13,    $13,    31          # shift left by 32 bits should be left all 0's

addi    $14,    $0,     0xFFFFFFFF  # initialize register 1 to all F's
sll     $14,    $14,    31          # shift left by 32 bits should be left all 0's

addi    $15,    $0,     0xFFFFFFFF  # initialize register 1 to all F's
sll     $15,    $15,    31          # shift left by 32 bits should be left all 0's

addi    $16,    $0,     0xFFFFFFFF  # initialize register 1 to all F's
sll     $16,    $16,    31          # shift left by 32 bits should be left all 0's

addi    $17,    $0,     0xFFFFFFFF  # initialize register 1 to all F's
sll     $17,    $17,    31          # shift left by 32 bits should be left all 0's

addi    $18,    $0,     0xFFFFFFFF  # initialize register 1 to all F's
sll     $18,    $18,    31          # shift left by 32 bits should be left all 0's

addi    $19,    $0,     0xFFFFFFFF  # initialize register 1 to all F's
sll     $19,    $19,    31          # shift left by 32 bits should be left all 0's

addi    $20,    $0,     0xFFFFFFFF  # initialize register 1 to all F's
sll     $20,    $20,    31          # shift left by 32 bits should be left all 0's

addi    $21,    $0,     0xFFFFFFFF  # initialize register 1 to all F's
sll     $21,    $21,    31          # shift left by 32 bits should be left all 0's

addi    $22,    $0,     0xFFFFFFFF  # initialize register 1 to all F's
sll     $22,    $22,    31          # shift left by 32 bits should be left all 0's

addi    $23,    $0,     0xFFFFFFFF  # initialize register 1 to all F's
sll     $23,    $23,    31          # shift left by 32 bits should be left all 0's

addi    $24,    $0,     0xFFFFFFFF  # initialize register 1 to all F's
sll     $24,    $24,    31          # shift left by 32 bits should be left all 0's

addi    $25,    $0,     0xFFFFFFFF  # initialize register 1 to all F's
sll     $25,    $25,    31          # shift left by 32 bits should be left all 0's

addi    $26,    $0,     0xFFFFFFFF  # initialize register 1 to all F's
sll     $26,    $26,    31          # shift left by 32 bits should be left all 0's

addi    $27,    $0,     0xFFFFFFFF  # initialize register 1 to all F's
sll     $27,    $27,    31          # shift left by 32 bits should be left all 0's

addi    $28,    $0,     0xFFFFFFFF  # initialize register 1 to all F's
sll     $28,    $28,    31          # shift left by 32 bits should be left all 0's

addi    $29,    $0,     0xFFFFFFFF  # initialize register 1 to all F's
sll     $29,    $29,    31          # shift left by 32 bits should be left all 0's

addi    $30,    $0,     0xFFFFFFFF  # initialize register 1 to all F's
sll     $30,    $30,    31          # shift left by 32 bits should be left all 0's

addi    $31,    $0,     0xFFFFFFFF  # initialize register 1 to all F's
sll     $31,    $31,    31          # shift left by 32 bits should be left all 0's
halt    

