.data
.text
    .globlmain
main:
    # Start Test
    addi    $1,     $0, -1  # verify that one can take common negative numbers
    addi    $2,     $0, -2  # verify that one can take common negative numbers
    addi    $3,     $0, -3  # verify that one can take common negative numbers
    addi    $4,     $0, -4  # verify that one can take common negative numbers
    addi    $5,     $0, -5  # verify that one can take common negative numbers
    addi    $6,     $0, -6  # verify that one can take common negative numbers
    addi    $7,     $0, -7  # verify that one can take common negative numbers
    addi    $8,     $0, -8  # verify that one can take common negative numbers
    addi    $9,     $0, -9  # verify that one can take common negative numbers
    addi    $10,    $0, -10 # verify that one can take common negative numbers
    addi    $11,    $0, -11 # verify that one can take common negative numbers
    addi    $12,    $0, -12 # verify that one can take common negative numbers
    addi    $13,    $0, -13 # verify that one can take common negative numbers
    addi    $14,    $0, -14 # verify that one can take common negative numbers
    addi    $15,    $0, -15 # verify that one can take common negative numbers
    addi    $16,    $0, -16 # verify that one can take common negative numbers
    addi    $17,    $0, -17 # verify that one can take common negative numbers
    addi    $18,    $0, -18 # verify that one can take common negative numbers
    addi    $19,    $0, -19 # verify that one can take common negative numbers
    addi    $20,    $0, -20 # verify that one can take common negative numbers
    addi    $21,    $0, -21 # verify that one can take common negative numbers
    addi    $22,    $0, -22 # verify that one can take common negative numbers
    addi    $23,    $0, -23 # verify that one can take common negative numbers
    addi    $24,    $0, -24 # verify that one can take common negative numbers
    addi    $25,    $0, -25 # verify that one can take common negative numbers
    addi    $26,    $0, -26 # verify that one can take common negative numbers
    addi    $27,    $0, -27 # verify that one can take common negative numbers
    addi    $28,    $0, -28 # verify that one can take common negative numbers
    addi    $29,    $0, -29 # verify that one can take common negative numbers
    addi    $30,    $0, -30 # verify that one can take common negative numbers
    addi    $31,    $0, -31 # verify that one can take common negative numbers
    # End Test

    # Exit program
    halt    
    li      $v0,    10
    syscall 
