.data
.text
    .globlmain
main:
    # Start Test
    add     $1,     $0, $0  # verify that one can clear registers and 0+0 works in the ALU
    add     $2,     $0, $0  # verify that one can clear registers and 0+0 works in the ALU
    add     $3,     $0, $0  # verify that one can clear registers and 0+0 works in the ALU
    add     $4,     $0, $0  # verify that one can clear registers and 0+0 works in the ALU
    add     $5,     $0, $0  # verify that one can clear registers and 0+0 works in the ALU
    add     $6,     $0, $0  # verify that one can clear registers and 0+0 works in the ALU
    add     $7,     $0, $0  # verify that one can clear registers and 0+0 works in the ALU
    add     $8,     $0, $0  # verify that one can clear registers and 0+0 works in the ALU
    add     $9,     $0, $0  # verify that one can clear registers and 0+0 works in the ALU
    add     $10,    $0, $0  # verify that one can clear registers and 0+0 works in the ALU
    add     $11,    $0, $0  # verify that one can clear registers and 0+0 works in the ALU
    add     $12,    $0, $0  # verify that one can clear registers and 0+0 works in the ALU
    add     $13,    $0, $0  # verify that one can clear registers and 0+0 works in the ALU
    add     $14,    $0, $0  # verify that one can clear registers and 0+0 works in the ALU
    add     $15,    $0, $0  # verify that one can clear registers and 0+0 works in the ALU
    add     $16,    $0, $0  # verify that one can clear registers and 0+0 works in the ALU
    add     $17,    $0, $0  # verify that one can clear registers and 0+0 works in the ALU
    add     $18,    $0, $0  # verify that one can clear registers and 0+0 works in the ALU
    add     $19,    $0, $0  # verify that one can clear registers and 0+0 works in the ALU
    add     $20,    $0, $0  # verify that one can clear registers and 0+0 works in the ALU
    add     $21,    $0, $0  # verify that one can clear registers and 0+0 works in the ALU
    add     $22,    $0, $0  # verify that one can clear registers and 0+0 works in the ALU
    add     $23,    $0, $0  # verify that one can clear registers and 0+0 works in the ALU
    add     $24,    $0, $0  # verify that one can clear registers and 0+0 works in the ALU
    add     $25,    $0, $0  # verify that one can clear registers and 0+0 works in the ALU
    add     $26,    $0, $0  # verify that one can clear registers and 0+0 works in the ALU
    add     $27,    $0, $0  # verify that one can clear registers and 0+0 works in the ALU
    add     $28,    $0, $0  # verify that one can clear registers and 0+0 works in the ALU
    add     $29,    $0, $0  # verify that one can clear registers and 0+0 works in the ALU
    add     $30,    $0, $0  # verify that one can clear registers and 0+0 works in the ALU
    add     $31,    $0, $0  # verify that one can clear registers and 0+0 works in the ALU
    # End Test

    # Exit program
    halt    