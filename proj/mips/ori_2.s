.data
.text
    .globlmain
main:
    # Start Test
    ori     $1,     $0, 0xF0F0  #Verify immediate assigned to register and $zero OR IMM succeeds in ALU
    ori     $2,     $0, 0xF0F0  #Verify immediate assigned to register and $zero OR IMM succeeds in ALU
    ori     $3,     $0, 0xF0F0  #Verify immediate assigned to register and $zero OR IMM succeeds in ALU
    ori     $4,     $0, 0xF0F0  #Verify immediate assigned to register and $zero OR IMM succeeds in ALU
    ori     $5,     $0, 0xF0F0  #Verify immediate assigned to register and $zero OR IMM succeeds in ALU
    ori     $6,     $0, 0xF0F0  #Verify immediate assigned to register and $zero OR IMM succeeds in ALU
    ori     $7,     $0, 0xF0F0  #Verify immediate assigned to register and $zero OR IMM succeeds in ALU
    ori     $8,     $0, 0xF0F0  #Verify immediate assigned to register and $zero OR IMM succeeds in ALU
    ori     $9,     $0, 0xF0F0  #Verify immediate assigned to register and $zero OR IMM succeeds in ALU
    ori     $10,    $0, 0xF0F0  #Verify immediate assigned to register and $zero OR IMM succeeds in ALU
    ori     $11,    $0, 0xF0F0  #Verify immediate assigned to register and $zero OR IMM succeeds in ALU
    ori     $12,    $0, 0xF0F0  #Verify immediate assigned to register and $zero OR IMM succeeds in ALU
    ori     $13,    $0, 0xF0F0  #Verify immediate assigned to register and $zero OR IMM succeeds in ALU
    ori     $14,    $0, 0xF0F0  #Verify immediate assigned to register and $zero OR IMM succeeds in ALU
    ori     $15,    $0, 0xF0F0  #Verify immediate assigned to register and $zero OR IMM succeeds in ALU
    ori     $16,    $0, 0xF0F0  #Verify immediate assigned to register and $zero OR IMM succeeds in ALU
    ori     $17,    $0, 0xF0F0  #Verify immediate assigned to register and $zero OR IMM succeeds in ALU
    ori     $18,    $0, 0xF0F0  #Verify immediate assigned to register and $zero OR IMM succeeds in ALU
    ori     $19,    $0, 0xF0F0  #Verify immediate assigned to register and $zero OR IMM succeeds in ALU
    ori     $20,    $0, 0xF0F0  #Verify immediate assigned to register and $zero OR IMM succeeds in ALU
    ori     $21,    $0, 0xF0F0  #Verify immediate assigned to register and $zero OR IMM succeeds in ALU
    ori     $22,    $0, 0xF0F0  #Verify immediate assigned to register and $zero OR IMM succeeds in ALU
    ori     $23,    $0, 0xF0F0  #Verify immediate assigned to register and $zero OR IMM succeeds in ALU
    ori     $24,    $0, 0xF0F0  #Verify immediate assigned to register and $zero OR IMM succeeds in ALU
    ori     $25,    $0, 0xF0F0  #Verify immediate assigned to register and $zero OR IMM succeeds in ALU
    ori     $26,    $0, 0xF0F0  #Verify immediate assigned to register and $zero OR IMM succeeds in ALU
    ori     $27,    $0, 0xF0F0  #Verify immediate assigned to register and $zero OR IMM succeeds in ALU
    ori     $28,    $0, 0xF0F0  #Verify immediate assigned to register and $zero OR IMM succeeds in ALU
    ori     $29,    $0, 0xF0F0  #Verify immediate assigned to register and $zero OR IMM succeeds in ALU
    ori     $30,    $0, 0xF0F0  #Verify immediate assigned to register and $zero OR IMM succeeds in ALU
    ori     $31,    $0, 0xF0F0  #Verify immediate assigned to register and $zero OR IMM succeeds in ALU
    # End Test

    # Exit program
    halt    
