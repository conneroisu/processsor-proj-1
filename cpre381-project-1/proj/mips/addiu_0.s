.data
.text
    .globlmain

main:
    #Start test
    addiu   $1,     $0, 1   #Ensure that basic positive integer addition works for each register
    addiu   $2,     $0, 2   #Ensure that basic positive integer addition works for each register
    addiu   $3,     $0, 3   #Ensure that basic positive integer addition works for each register
    addiu   $4,     $0, 4   #Ensure that basic positive integer addition works for each register
    addiu   $5,     $0, 5   #Ensure that basic positive integer addition works for each register
    addiu   $6,     $0, 6   #Ensure that basic positive integer addition works for each register
    addiu   $7,     $0, 7   #Ensure that basic positive integer addition works for each register
    addiu   $8,     $0, 8   #Ensure that basic positive integer addition works for each register
    addiu   $9,     $0, 9   #Ensure that basic positive integer addition works for each register
    addiu   $10,    $0, 10  #Ensure that basic positive integer addition works for each register
    addiu   $11,    $0, 11  #Ensure that basic positive integer addition works for each register
    addiu   $12,    $0, 12  #Ensure that basic positive integer addition works for each register
    addiu   $13,    $0, 13  #Ensure that basic positive integer addition works for each register
    addiu   $14,    $0, 14  #Ensure that basic positive integer addition works for each register
    addiu   $15,    $0, 15  #Ensure that basic positive integer addition works for each register
    addiu   $16,    $0, 16  #Ensure that basic positive integer addition works for each register
    addiu   $17,    $0, 17  #Ensure that basic positive integer addition works for each register
    addiu   $18,    $0, 18  #Ensure that basic positive integer addition works for each register
    addiu   $19,    $0, 19  #Ensure that basic positive integer addition works for each register
    addiu   $20,    $0, 20  #Ensure that basic positive integer addition works for each register
    addiu   $21,    $0, 21  #Ensure that basic positive integer addition works for each register
    addiu   $22,    $0, 22  #Ensure that basic positive integer addition works for each register
    addiu   $23,    $0, 23  #Ensure that basic positive integer addition works for each register
    addiu   $24,    $0, 24  #Ensure that basic positive integer addition works for each register
    addiu   $25,    $0, 25  #Ensure that basic positive integer addition works for each register
    addiu   $26,    $0, 26  #Ensure that basic positive integer addition works for each register
    addiu   $27,    $0, 27  #Ensure that basic positive integer addition works for each register
    addiu   $28,    $0, 28  #Ensure that basic positive integer addition works for each register
    addiu   $29,    $0, 29  #Ensure that basic positive integer addition works for each register
    addiu   $30,    $0, 30  #Ensure that basic positive integer addition works for each register
    addiu   $31,    $0, 31  #Ensure that basic positive integer addition works for each register

    halt    